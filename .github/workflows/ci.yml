name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Static checks are Python-version independent: ruff is pinned in
  # pyproject.toml and mypy is configured for Python 3.11. Run them once.
  quality:
    name: quality
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5

      - name: Run pre-commit hooks (ruff format + lint, mypy --strict)
        uses: ./.github/actions/flox-command
        with:
          command: pre-commit run --all-files --show-diff-on-failure

  # Run the full default suite once with coverage. The e2e tests exercise
  # orchestration behavior, so avoid repeating them across every Python leg.
  test-linux:
    name: test-linux (py3.11, coverage)
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5

      - name: pytest (unit + e2e, coverage)
        uses: ./.github/actions/flox-command
        with:
          command: pytest -v -m "unit or e2e" --cov=eden --cov-fail-under=70

  # Compatibility legs keep the supported-Python matrix on Linux without
  # duplicating the e2e suite or coverage collection.
  test-linux-compat:
    name: test-linux-compat (py${{ matrix.python }})
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        python: ["3.12", "3.13"]
    env:
      EDEN_PYTHON: python${{ matrix.python }}
    steps:
      - uses: actions/checkout@v5

      - name: pytest (unit)
        uses: ./.github/actions/flox-command
        with:
          eden-python: python${{ matrix.python }}
          command: pytest -v -m unit

  # macOS catches platform-specific path/process behavior with unit tests only;
  # Linux owns the e2e and coverage gates.
  test-macos:
    name: test-macos (py3.13)
    needs: quality
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - name: pytest (unit)
        uses: ./.github/actions/flox-command
        with:
          eden-python: python3.13
          command: pytest -v -m unit

  # Flox is Linux/macOS only, but eden ships Windows-specific path handling.
  # This leg stays native and runs unit tests only; Linux owns e2e coverage.
  test-windows:
    name: test-windows (py3.13)
    needs: quality
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python 3.13
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package with dev extras
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: pytest (unit)
        run: pytest -v -m unit

  # Real provider tests are Linux-only and heavily capability-gated. Run them
  # once instead of once per Python version.
  integration:
    name: integration (py3.13)
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5

      - name: pytest (integration)
        uses: ./.github/actions/flox-command
        with:
          eden-python: python3.13
          command: pytest -v -m integration
